# Changesets

This folder is managed by `@changesets/cli`. It stores changelog entries for each change that has not yet been released.

To add a new changeset, run `pnpm changeset` from the repository root.
